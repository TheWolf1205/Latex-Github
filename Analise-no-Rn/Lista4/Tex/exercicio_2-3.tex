\begin{homeworkProblem}[Seção 2-3]
  Seja
\[
f:M\longrightarrow\mathbb{R}
\]
uma função real de classe $C^{k}$ definida em uma superfície $M$
de classe $C^{k}$ de dimensão $m+1$.

Prove que se $a\in\mathbb{R}$ é um valor regular de $f$ então o conjunto
\[
N=\{x\in M: f(x)\le a\}
\]
é uma superfície com bordo de classe $C^{k}$, com dimensão $m+1$ e
\[
\partial N=f^{-1}(a).
\]
  \begin{solution}
    XXX  
  \end{solution}
\end{homeworkProblem}
