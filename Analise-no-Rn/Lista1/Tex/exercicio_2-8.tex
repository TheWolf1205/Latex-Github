\begin{homeworkProblem}[Seção 2-8]
  \begin{enumerate}
        \item[(a)] Sejam $U \subset \mathbb{R}^m$, $V \subset \mathbb{R}^n$ abertos e $f : U \to \mathbb{R}^n$, $g : V \to \mathbb{R}$ diferenciáveis, com $f(U) \subset V$. Para todo $x \in U$, pondo $y = f(x)$ tem-se
        \[
        \nabla (g \circ f)(x) = [f'(x)]^* \cdot \nabla g(y).
        \]

        \item[(b)] Interprete a igualdade
        \[
        \nabla_x z = \sum_i \frac{\partial z}{\partial y_i} \nabla_x y_i,
        \]
        escrita na notação clássica, e identifique-a como o resultado anterior.
    \end{enumerate}
  \begin{solution}
    Solução.
  \end{solution}
\end{homeworkProblem}
