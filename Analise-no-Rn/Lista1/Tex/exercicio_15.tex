\begin{homeworkProblem}[Seção 2-6]
  Seja $f : U \to \mathbb{R}^m$ diferenciável no aberto $U \subset \mathbb{R}^m$. Se $|f(x)|$ é constante quando $x$ varia em $U$, então o determinante jacobiano de $f$ é identicamente nulo.
  \begin{solution}
    Solução.
  \end{solution}
\end{homeworkProblem}
