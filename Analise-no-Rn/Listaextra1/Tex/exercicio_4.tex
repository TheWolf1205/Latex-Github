\begin{homeworkProblem}
  Seja $M \subset \mathbb{R}^n$ uma superfície de classe $C^k$, $k \geq 2$, e dimensão $m$. Considere o conjunto
    \[
    TM^\perp = \{(x,v) \in \mathbb{R}^n \times \mathbb{R}^n : x \in M, \, v \in (T_x M)^\perp\}.
    \]
    Mostre que $TM^\perp$ é uma superfície de classe $C^{k-1}$ e dimensão $n$ (chamada o fibrado normal de $M$). Prove que a aplicação $\pi : TM^\perp \to M$, $\pi(x,v)=x$, é uma submersão de classe $C^{k-1}$ e que para todo ponto de $M$ tem uma vizinhança aberta $V$ tal que $\pi^{-1}(V)$ é difeomorfo ao produto $V \times \mathbb{R}^{n-m}$.
  \begin{solution}
    XXX  
  \end{solution}
\end{homeworkProblem}
