\begin{homeworkProblem}
  Seja $M \subset \mathbb{R}^n$ uma superfície de classe $C^k$ ($k \geq 1$) e dimensão $m$. Considere o conjunto
    \[
    TM = \{(x,v) \in \mathbb{R}^n \times \mathbb{R}^n : x \in M, \, v \in T_x M\}.
    \]
    Mostre que $TM$ é uma superfície de dimensão $2m$ e classe $C^{k-1}$ (conhecida como fibrado tangente de $M$).

    Se $N$ é outra superfície e $F : M \to N$ é uma aplicação de classe $C^k$, defina a aplicação tangente $TF : TM \to TN$ pondo
    \[
    (TF)(x,v) = (F(x), F'(x)\cdot v)
    \]
    e mostre que $TF$ é de classe $C^{k-1}$.
  \begin{solution}
    XXX  
  \end{solution}
\end{homeworkProblem}
